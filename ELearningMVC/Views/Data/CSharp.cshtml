@model IEnumerable<ELearningMVC.Models.Subject>

@{
    ViewBag.Title = "CSharp";
}

    <h1>C Sharp </h1>
    <p> Data Will be display from the Database</p>
<div class="container">
    <div class=row>
        @foreach (var item in Model)
        {
            <div class="col-md-12">
                <h1> @Html.DisplayFor(modelItem => item.Category)</h1>
                <h2>
                    @Html.DisplayFor(modelItem => item.Material)
                </h2>

            </div>

        }


    </div>

</div>
